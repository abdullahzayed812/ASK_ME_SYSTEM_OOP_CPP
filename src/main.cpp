
#include <iostream>
#include <vector>

#include "../include/helper.hpp"
#include "../include/question.hpp"

int main() {
  std::string line;
  std::getline(std::cin, line);
  std::getline(std::cin, line);

  std::cout << line << "\n";

  return 0;
}